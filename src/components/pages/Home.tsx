import { Box } from '@chakra-ui/react';
import ListingTemplate from '@templates/ListingTemplate';

const Home = () => {
  return (
    <ListingTemplate>
      <Box h="300vh">Hey</Box>
    </ListingTemplate>
  );
};

export default Home;
